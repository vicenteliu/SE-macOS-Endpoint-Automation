# Phase 3 — Software distribution

> Network to installed, updated, and inventoried

🚧 *Hops and markers settled. Prerequisites, steps, verification and acceptance fill in per hop.*

| # | Hop | Marker | What it answers |
|---|---|---|---|
| 1 | **Repository model** | 🔨 lab | The server is a web server; the client decides. Catalogs, manifests, package metadata |
| 2 | **Blocking and version comparison** | 🔨 lab | One metadata array does two jobs — and the second is what happens when the user is inside the application |
| 3 | **Packaging automation** | 🔨 lab | Processor chains, overrides, and trust pinning as a supply-chain control |
| 4 | **Package internals** | 🧭 | Payload vs payload-free, install scripts, signing, notarisation |

## Prerequisites

*(What must already be true before this phase runs. To be written.)*

## Verification

Per hop: **the command, and the expected output.** A hop with no stated acceptance is not
designed, it is described.

## Acceptance

*(To be written.)*

## Escape Hatch

*(What a user does when this phase blocks legitimate work, and what the exception costs.
Every phase has one — see [`../../docs/00-the-chain.md`](../../docs/00-the-chain.md).)*
